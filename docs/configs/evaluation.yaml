# Stage 5 · Evaluation — DRAFT. Produces one BEAM file per biopsy per model.

bundle: prostate_combined_v1__sHE__raw__conch
model:  results/experiments/ki67_stain_comparison/sweep/he_lr1e4   # checkpoints, folds, arch
subset: holdout                   # holdout | development (CV test) | all
evaluation_tag: holdout_v1

attention: [raw, sigmoid, rank]   # attention variants written to BEAM
include_embeddings: false         # store embeddings in BEAM, or reference the bundle

output_dir: results/evaluation/he_lr1e4__holdout_v1
