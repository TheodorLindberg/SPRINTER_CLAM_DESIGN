# Stage 5 · Evaluation — DRAFT
# See design/07-evaluation.md.

bundle: data/bundles/example_dataset_v1__heldout
model: runs/training/ki67_avg_clam      # checkpoints, folds, architecture
evaluation_tag: heldout_v1              # readable run name

attention: [raw, sigmoid, rank]         # which attention variants to write to BEAM
include_embeddings: false               # store embeddings in BEAM, or reference bundle

output_dir: runs/evaluation/ki67_avg_clam__heldout_v1
# Produces one BEAM file per biopsy per model: {biopsy_id}__{model_id}.beam.h5
