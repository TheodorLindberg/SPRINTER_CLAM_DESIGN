# Stage 6 · `heatmaps.yaml`

Underlay source variant, attention type and colormap, and render/output options. Mind the raw-frame coordinate caveat in [Stage 6 · Heatmaps](../design/08-heatmaps.md) — attention coordinates are pushed through the chosen variant's transformation matrix before overlay.

```yaml title="heatmaps.yaml"
--8<-- "heatmaps.yaml"
```
