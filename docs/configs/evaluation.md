# Stage 5 · `evaluation.yaml`

Which bundle and model to evaluate, the **cohort scope** to score (`holdout` for the final locked test, `development` for CV-test predictions), which attention variants to write into the [BEAM files](../formats/beam.md), and where to put them. See [Stage 5 · Evaluation](../design/07-evaluation.md).

```yaml title="evaluation.yaml"
--8<-- "evaluation.yaml"
```
