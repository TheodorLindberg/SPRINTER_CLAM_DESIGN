# Split registry — DRAFT
# Names a dataset and the seeds all models share, so fold splits are reproducible
# and can be referenced later by name. See design/06-model-training.md.

dataset_id: example_dataset_v1
patient_count: 119          # if this changes, the pipeline warns that splits are stale

fold_seeds: [0, 1, 2, 3, 4]
model_seeds: [100, 200, 300]

n_folds: 5
split_level: patient        # patient | biopsy | dataset
