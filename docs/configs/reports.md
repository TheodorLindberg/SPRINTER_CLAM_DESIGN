# Reports — `reports.yaml`

Controls the report layer: plot backend, the standalone stylesheet, the hybrid layout, **HPO segregation** (its own index + how many HPO checkpoints to keep), and table export. See [Reports](../design/11-reports.md).

```yaml title="reports.yaml"
--8<-- "reports.yaml"
```
